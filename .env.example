NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: service role key for server-only use (never expose to client)
SUPABASE_SERVICE_ROLE_KEY=

# Database connection string (if using migrations/local tooling)
DATABASE_URL=